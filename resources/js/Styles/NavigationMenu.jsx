import styled from "styled-components";

const NavigationMenu = styled.ul`
    display: flex;
    margin-left: auto;
    gap: 1.5rem;

    li {
        list-style: none;
    }
`;

export default NavigationMenu;
